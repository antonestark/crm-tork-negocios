
// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';
// Assuming database.types.ts is generated and correctly placed relative to this file
// If not, the import path might need adjustment or removal if types are directly from @supabase/supabase-js
// import './database.types'; // Import our extended types - Commenting out if types are standard or handled differently

// Read Supabase URL and Anon Key from environment variables (Vite convention)
const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

// Basic validation to ensure environment variables are set
if (!supabaseUrl) {
  throw new Error("VITE_SUPABASE_URL is not set. Please check your .env file or environment variables.");
}
if (!supabaseAnonKey) {
  throw new Error("VITE_SUPABASE_ANON_KEY is not set. Please check your .env file or environment variables.");
}

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

// Initialize the Supabase client
// Use Database type if it's correctly defined and generated, otherwise remove <Database>
export const supabase = createClient<Database>(supabaseUrl, supabaseAnonKey);
