
import { ServiceForm as RefactoredServiceForm } from './service-form';

// Re-export the refactored component to maintain compatibility with existing imports
export const ServiceForm = RefactoredServiceForm;

export default ServiceForm;
