
import { MaintenanceForm as RefactoredMaintenanceForm } from './maintenance-form';

// Re-export the refactored component to maintain compatibility with existing imports
export const MaintenanceForm = RefactoredMaintenanceForm;

export default MaintenanceForm;
