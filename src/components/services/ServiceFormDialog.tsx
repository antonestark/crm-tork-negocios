
import { ServiceFormDialog as RefactoredServiceFormDialog } from './service-form';

// Re-export the refactored component to maintain compatibility with existing imports
export const ServiceFormDialog = RefactoredServiceFormDialog;

export default ServiceFormDialog;
